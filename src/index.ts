const array = [1, 2, 3, 4, 5];
const indiceParaRemover = 2; // por exemplo, queremos remover o elemento com índice 2

array.splice(indiceParaRemover, 1);

console.log(array); // O elemento com índice 2 (que é o número 3) será removido
